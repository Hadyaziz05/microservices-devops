# Terraform state files and backups
*.tfstate
*.tfstate.backup

# Crash log files
crash.log
crash.*.log

# .terraform directory (contains provider plugins, modules, etc.)
.terraform/

# Lock file (you may want to version this in some cases)
.terraform.lock.hcl

# Override files (sometimes used for local config overrides)
*.tfvars
*.tfvars.json

# Sensitive variable files (if any)
secret.tfvars
terraform.tfvars
terraform.tfvars.json

# Plan output
*.tfplan
